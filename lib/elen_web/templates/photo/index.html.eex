<%= link "New Photo", to: Routes.photo_path(@conn, :new) %>

<h1>Photos</h1>
<%= for photo <- @photos do %>
  <%= Elen.Photo.url({photo.photo, photo}, :thumb)
    |> img_tag()
    |> link(to: Routes.photo_path(@conn, :show, photo))
  %>
<% end %>
